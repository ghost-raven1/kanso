# Миграция

Цель первого мигратора — собственный функциональный React + Vite код в поддерживаемом подмножестве Kanso. Проверка консервативна: неизвестная совместимость блокирует запись.

1. Установить зависимости исходного приложения, чтобы проверить реальный package graph.
2. Выполнить `kanso migrate --check` (или локальный CLI из README).
3. Разрешить все блокирующие диагностики; повторить проверку.
4. Выполнить `--apply`, проверить обычный diff.
5. Выполнить `npm install`, TypeScript, сборку и тесты пользовательских сценариев.

| Диагностика | Следующее действие |
| --- | --- |
| `REACT_IMPORT` | Заменить React require/dynamic import/re-export явным поддерживаемым ESM import; namespace API проверить отдельно |
| `REACT_DEPENDENCY` | Заменить/портировать библиотеку; адаптера React внутри Kanso нет |
| `DEPENDENCY_AUDIT` | Установить зависимость и обеспечить статически проверяемый runtime entry |
| `UNSUPPORTED_API` / `REACT_DOM` | Убрать/портировать API вне публичного контракта |
| `CLASS_COMPONENT` | Переписать наследника React Component/PureComponent в функцию; обычные классы разрешены |
| `STATE_SNAPSHOT` | Проверить повторные записи одного состояния и чтения этого состояния после setter |
| `ASYNC_SNAPSHOT` / `CALLBACK_SNAPSHOT` | Зафиксировать нужный снимок локальной переменной либо принять live reads |
| `EFFECT_TRACKING` | Явно выбрать deps вместо React-эффекта после каждого render |
| `CUSTOM_HOOK` | Дать статически доступную локальную реализацию или портировать внешний hook |
| `KANSO_HOOK_*` | Использовать статические patterns/imports, const-результат и один терминальный return |
| `PATH_ALIAS` / `DYNAMIC_IMPORT` | Использовать tsconfig paths или статические resolve.alias; computed imports требуют ручного порта |
| `KANSO_PURITY` | Явно оформить чистое вычисление через useMemo либо действие через useEffect |
| `KANSO_LIST_KEY` / `KANSO_LIST_BODY` | Дать стабильный key, вынести setup строки в компонент |
| `KANSO_SPREAD_KEY` | Вынести key из spread в явный JSX-атрибут |
| `KANSO_RAW_HTML_CHILDREN` | Выбрать dangerouslySetInnerHTML или children для одного элемента |
| `VITE_CONFIG_DYNAMIC` / `VITE_CONFIG_CYCLE` | Выбрать статический конфиг; разорвать цикл переэкспортов |

Локальные пользовательские hooks проверяются через отдельные файлы и цепочки переэкспорта, включая aliases. Поддерживаются `.ts`/`.js`, scalar/tuple/object-результаты и вложенные вызовы hooks. Циклический или неизвестный контракт не считается доказанной совместимостью. Проверяются также мигрируемые реализации hooks, их зависимости и изменения snapshot-семантики; повторные вызовы возвращённых методов считаются потенциальными изменениями состояния и требуют проверки. Это консервативный анализ, возможны блокировки кода, который автор может безопасно портировать вручную.

Формы props и event types основаны на DOM/Solid. Сложные типы React не подменяются `any`; неподдерживаемые импорты типов требуют явного порта. Named/default React imports преобразуются, включая JSX Fragment и поддерживаемые qualified types. Entry `root.render(<App />)` становится `root.render(() => <App />)`, чтобы владелец создавался до JSX.

Изменения source/package/config сначала собираются в памяти. Ошибка анализа оставляет приложение неизменным. Перед записью проверяется отсутствие concurrent edits. Файлы заменяются через temporary+rename; при ошибке записи собственные записанные изменения откатываются. Это транзакция в рамках работы процесса, а не гарантия атомарности нескольких файлов при аварийном отключении питания.

Проверяется достижимый граф приложения. Неиспользуемый исходный файл не считается перенесённым. Поддержаны tsconfig paths/baseUrl/extends и статические resolve.alias Vite. Alias-резолверы произвольных плагинов, computed imports, React-классы и непроверенные внешние source workspaces автоматическим переносом не покрываются.

Мигратор не запускает React для неподдерживаемого кода. CLI не устанавливает пакеты и не выполняет скрипты исходного приложения; это отдельный проверяемый шаг после diff.

## Графы приложений в 0.6.1

Для нескольких клиентских/серверных точек входа перечислите исходники или HTML-файлы. Обе опции можно повторять:

```bash
kanso migrate --check --entry index.html --entry src/entry-server.tsx --json
kanso migrate --check --config vite.config.ts --config vite.admin.config.ts
```

Без `--entry` мигратор читает статический `build.rollupOptions.input`, строковый `build.ssr` либо `index.html` относительно Vite root. Из HTML учитываются все внешние module scripts. Inline modules нужно сначала вынести в файл. Явные `--entry` задают общий набор для каждого выбранного конфига.

Default/named переэкспорты конфигурации и импортированный конфиг разрешаются до файла с объектом настроек. Изменения plugins и `resolve.tsconfigPaths` вносятся туда; промежуточные re-export файлы сохраняются. `doctor` проверяет ту же цепочку. Код конфигурации не исполняется. Динамические значения и циклы блокируют запись, но доступные исходники продолжают анализироваться.

`coverage` в JSON содержит `entries`, `configs`, `files` и `complete`. Полнота относится к выбранному графу, а не ко всему репозиторию. Неполный обход или конфликт преобразований разных конфигов запрещает `--apply`. При недоступной зависимости отчёт показывает неполный охват; отсутствие остальных ошибок не означает совместимость непроверенного кода.

Локальные пакеты внутри корня проекта поддерживаются через `file:` и реальные пути установленных workspace symlinks: явные ESM exports, subpaths, barrels и custom hooks используют один resolver. Декларации типов не заменяют runtime-исходники. Условия `browser`/`node`, wildcard exports и источники за пределами корня требуют явного выбора source alias или отдельного порта. React-зависимости и package.json внутренних пакетов не переписываются автоматически: их нужно портировать отдельно. Код с блокирующими зависимостями остаётся неизменным.

Изменения независимых состояний больше не считаются конфликтом snapshot-семантики. Повторные записи одного состояния, чтения после его setter и отложенные чтения по-прежнему требуют явной проверки. Мигратор не угадывает намерение автора.

Дальнейшие возможности и критерии их приёмки перечислены в [плане подготовки миграции](migration-readiness.md).

## Изменения 0.4

Context поддерживает реактивный Provider.value; добавлены useId, ComponentProps, CSSProperties и RefObject. Вложенные параметры/результаты hooks и object/array rest переносятся без ручного уплощения. Анализ учитывает реальные bindings, поэтому одноимённая локальная переменная не считается состоянием другого компонента.

Ошибки содержат исходный код KANSO_*, строку/столбец и пример исправления. После установки зависимостей выполните kanso doctor. Пакеты и скомпилированные hooks обновляются одной версией; не смешивайте ABI разных релизов. [Примеры, HMR и шаблоны](dx.md).

## Веб-слой 0.5

Изменений существующего приложения не требуется. Для native POST замените ручной fetch на `useForm`/`Form`, возвращайте проверенные `values` вместе с `errors`, а на странице привяжите `value={form.values.name ?? ''}`. Не возвращайте целиком FormData: пароли и другие непредназначенные для отображения сведения должны оставаться на сервере.

`pending` теперь включает revalidation. Ошибка обновления доступна отдельно через `useRevalidator`; повторный refresh не повторяет сохранение. При смене URL pending-запрос формы отменяется, её состояние очищается. Старые `useForm<T>(routeId?)` и `Record<string, RouteHandlers>` поддерживаются.

Можно постепенно перейти на `defineRouteHandlers`, `routeUrl`, `RouteParams` и `LoaderData`. Серверные типы импортируются через `import type`; runtime-import по-прежнему запрещён. Новые преобразования мигратора для роутинга или форм в 0.5 не добавлены. См. [полный контракт](web-apps.md).

## Внешнее состояние

Начиная с 0.7 внешние stores с `getState` / `subscribe` можно подключать через `useStore`; `defineService` создаёт экземпляры на приложение и SSR-запрос. [Пример с Zustand vanilla](services.md). Перенос React hooks стороннего store выполняется явно. Проверенный vanilla entry пакета с optional React peer проходит аудит по правилам ниже.

## Vanilla dependencies

В 0.7.2 `migrate` и `doctor` различают используемые runtime entries пакетов с **необязательным** React peer. Например:

```ts
import { createStore } from 'zustand/vanilla'; // независимый store
// import { useStore } from 'zustand';        // React hook: REACT_DEPENDENCY
```

Исключение выдаётся после проверки установленных JS-файлов выбранного entry. Учитываются статические imports, re-exports, литеральные `import()` и CommonJS `require()`, package imports и wildcard exports. Проверяются все runtime-ветки условий exports, включая browser/node/import/require/development/production, а также browser replacements. Это консервативная проверка: даже неактивная в текущей сборке React-ветка запрещает исключение. Правила [условных exports](https://nodejs.org/api/packages.html#conditional-exports) не сводятся к одному `main` или файлу типов.

Проверка не исполняет код пакета или конфигурации. `import type` и декларации не считаются runtime-доказательством. Вычисляемые imports, `import.meta.glob`, динамический `require`, custom module loaders, отсутствующие targets и неподдерживаемые файлы дают `DEPENDENCY_AUDIT`; миграция ничего не записывает. Литеральные прямые imports позволяют сделать граф проверяемым.

Успешная проверка одного subpath не разрешает остальные imports этого пакета. Обязательный React peer или React в dependencies по-прежнему блокирует миграцию. Неиспользуемый пакет с optional React peer проверяется по корневому entry: удалите ненужную зависимость, если он требует React. Общая проверка пакетов без optional React peers остаётся на уровне их manifests.

`doctor` использует тот же source resolver, что и мигратор: HTML/статические Vite entries, aliases, tsconfig paths и barrels. Для исключения необходим проверяемый граф приложения. Это не автоматическая замена React hooks: перенесите binding на `@kanso/core` `useStore`, сохранив vanilla store. После review diff установите зависимости, выполните `doctor`, TypeScript, сборку и пользовательские сценарии.

## Сложные конфиги и source mappings (0.8.0)

Мигратор читает прямые object-returning callbacks `defineConfig(({ command }) => ({ … }))`, сохраняя callback в результате. Значение `base` может зависеть от command/env: оно сохраняется без вычисления и не выбирает исходные модули. `root`, entries и aliases должны оставаться статически разрешимыми. Проверка графа не подтверждает правильность deployment-настроек. Импорты `URL`/`fileURLToPath` из `node:url` и локальные чистые однопараметрические helper-функции поддерживаются:

```ts
const source = (path: string) => fileURLToPath(new URL(path, import.meta.url));
```

Конфиг не исполняется. Многошаговые helpers, циклы, затенённые функции, computed aliases, динамический граф и исполняемые top-level statements продолжают блокировать запись. Для разных приложений явно задавайте `--config` и `--entry`.

Для обследования federation-кода можно сопоставить удалённый импорт доступному исходнику:

```bash
kanso migrate --check --entry src/main.tsx \
  --source-alias account/hooks=./src/account/hooks.ts --json
```

Это точное сопоставление одного specifier, общее для обхода исходников и проверки custom hooks. Файл остаётся в графе с настоящим filesystem path. Сопоставления не конвертируют federation runtime и не меняют импорты. `--apply` с source mappings блокируется диагностикой `SOURCE_ALIAS_PORT`, пока пользователь не перенесёт runtime-конфигурацию и реальные imports. Неполный граф и React-зависимые библиотеки по-прежнему блокируют миграцию.

Поддержаны [layout-эффекты, component refs, imperative handles и чистые вычисления в map](lifecycle.md). Мигратор не угадывает snapshot-семантику и не переносит произвольные React-библиотеки.
